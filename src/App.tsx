import { Routes, Route } from 'react-router-dom'

import './styles/globals.css'

const App = () => {
  return (
    <main className="flex h-screen">
      <Routes>
        {/* Public Route */}
        {/* Private Route */}
      </Routes>
    </main>
  )
}

export default App
